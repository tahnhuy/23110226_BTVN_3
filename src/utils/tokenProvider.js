// TODO: Logic tạo Access Token và Refresh Token sử dụng jsonwebtoken
const jwt = require('jsonwebtoken');

const generateTokens = (payload) => {
    // Logic ký token ở đây
};

module.exports = { generateTokens };
